---
const offre = Astro.props;
---


    <p>{offre.adresse}</p>
    <p>{offre.favori}</p>
    <p>{offre.id}</p>
    <p>{offre.nnbChambres}</p>
    <p>{offre.nbSdb}</p>
    <p>{offre.nomMaison}</p>
    <p>{offre.prix}</p>
    <p>{offre.surface}</p>
    <img>{offre.imgUrl}</img>
